node_modules/
.DS_Store
files.zip

# env
.env
.env.local
*.env

# backend
backend/dist/
backend/generated/

# mobile (Expo)
mobile/.expo/
mobile/dist/
mobile/web-build/
mobile/*.jks
mobile/*.p8
mobile/*.p12
mobile/*.key
mobile/*.mobileprovision
# (mobile/android and mobile/ios are already ignored by mobile/.gitignore — generated output.)
# Firebase/Google service-account private keys (FCM V1 push credential). A live secret:
# anyone holding it can send push notifications as this app.
mobile/*service-account*.json
mobile/*serviceaccount*.json
mobile/*-firebase-adminsdk-*.json

# web builds
web-panel/dist/
admin/dist/

# logs
*.log
npm-debug.log*
